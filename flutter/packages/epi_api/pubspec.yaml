name: epi_api
description: EPI Controle — cliente HTTP para a API UBX (Python backend).
version: 0.1.0

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.22.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  retrofit: ^4.9.2
  json_annotation: ^4.9.0
  flutter_secure_storage: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.13
  retrofit_generator: ^10.2.6
  json_serializable: ^6.8.0
