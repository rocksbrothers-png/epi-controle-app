# Ambiente e segredos
.env
.env.*
*.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.py[cod]

# Ambiente virtual
.venv/
venv/
env/

# Logs
*.log

# Node / frontend
node_modules/
dist/
build/

# IDE / editor
.vscode/
.idea/

# Sistema operacional
.DS_Store
Thumbs.db

# Testes / cache
.pytest_cache/
.mypy_cache/
.coverage

# Render / deploy (opcional)
.render/

# Outros temporários
*.tmp
*.bak

# Import debug artifacts
logs/imports/

# Claude Code internal (worktrees, session data)
.claude/

# Flutter — saidas de build local.
#
# Estes caminhos sao regenerados por `melos bootstrap` / `flutter pub get` e
# `flutter gen-l10n`, inclusive no CI. Sem estas linhas, rodar os testes do app
# localmente sujava o `git status` com cache de build e caminhos absolutos da
# maquina de quem rodou.
#
# `local.properties` fica de fora de proposito: ja e versionado e aponta para o
# SDK de quem trabalha no projeto — ignora-lo agora nao o destrackearia, e
# remove-lo apagaria o arquivo de quem o usa.
flutter/**/.dart_tool/
flutter/pubspec.lock

# Android — material de assinatura (issue #200).
#
# O keystore e o `key.properties` nascem dentro do runner a partir dos secrets
# e morrem com ele; localmente, quem gera uma chave de upload para testar o
# build de release não pode commitá-la por acidente. Chave de upload perdida ou
# vazada custa o controle sobre as atualizações do app já publicado.
#
# Ver docs/ci-cd/ANDROID_SIGNING.md.
*.jks
*.keystore
**/key.properties
